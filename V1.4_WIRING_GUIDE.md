# v1.4 WIRING GUIDE - HƯỚNG DẪN NỐI MẠCH CHI TIẾT

**Version:** v1.4 COMPREHENSIVE OVERHAUL
**Date:** 2025-11-22
**Mục đích:** Hướng dẫn nối dây TRÁNH XUNG ĐỘT, ĐẢM BẢO ỔN ĐỊNH

---

## 🔴 CRITICAL: ĐỌC KỸ TRƯỚC KHI NỐI DÂY!

### ⚠️ NGUY HIỂM: Level Shifting 5V ↔ 3.3V

**ESP8266 CHỈ CHỊU ĐƯỢC 3.3V!**
- ❌ **TUYỆT ĐỐI KHÔNG** nối trực tiếp UNO TX (5V) → ESP8266 RX (GPIO3)
- ✅ **BẮT BUỘC** phải dùng voltage divider hoặc level shifter
- ⚡ Nếu không: **HƯ HỎNG ESP8266 VĨNH VIỄN**

---

## 📌 PHẦN 1: NỐI UART UNO ↔ ESP8266 (QUAN TRỌNG NHẤT!)

### 🔧 Thiết Bị Cần Thiết

1. **Voltage Divider (Đơn giản nhất - Khuyên dùng)**
   - 2 điện trở: 1kΩ + 2kΩ (hoặc 2.2kΩ)
   - Total cost: ~500đ

2. **Level Shifter Module (Tốt nhất - Khuyên dùng cho production)**
   - Module 4-channel bi-directional level shifter
   - IC: TXS0108E hoặc tương tự
   - Total cost: ~15,000đ

---

### 🎯 OPTION 1: Voltage Divider (Đơn giản - Cho Test)

```
UNO D1 (TX) ───┬─── 1kΩ ───┬─── ESP8266 GPIO3 (RX)
               │            │
               └─── GND     └─── 2kΩ ─── GND

Công thức: Vout = Vin × R2/(R1+R2)
         = 5V × 2kΩ/(1kΩ+2kΩ)
         = 5V × 0.667
         = 3.33V ✅ SAFE!
```

**Ưu điểm:**
- ✅ Đơn giản, rẻ
- ✅ Không cần nguồn 5V riêng

**Nhược điểm:**
- ⚠️ Chỉ 1 chiều (UNO → ESP8266)
- ⚠️ Không dùng được cho I2C

**Nối Dây:**
```
1. UNO D1 (TX)     → 1kΩ resistor → Node A
2. Node A          → ESP8266 GPIO3 (RX)
3. Node A          → 2kΩ resistor → GND
4. ESP8266 GPIO1 (TX) → UNO D0 (RX) [TRỰC TIẾP - OK!]
5. UNO GND         → ESP8266 GND   [COMMON GROUND BẮT BUỘC]
```

**LƯU Ý:**
- ESP8266 TX → UNO RX: 3.3V → 5V **OK** (5V logic nhận 3.3V là HIGH)
- UNO TX → ESP8266 RX: 5V → 3.3V **DANGER** (cần voltage divider)

---

### 🎯 OPTION 2: Level Shifter Module (Khuyên Dùng - Production)

**Module TXS0108E 4-Channel Bi-directional:**

```
┌─────────────────────────┐
│  Level Shifter Module   │
├─────────────────────────┤
│ HV (High Voltage Side)  │ ← 5V từ UNO
│   HV1 ← UNO D1 (TX)     │
│   HV2 → UNO D0 (RX)     │
│   GND ← UNO GND         │
├─────────────────────────┤
│ LV (Low Voltage Side)   │ ← 3.3V từ ESP8266
│   LV1 → ESP8266 GPIO3   │
│   LV2 ← ESP8266 GPIO1   │
│   GND → ESP8266 GND     │
└─────────────────────────┘
```

**Nối Dây Chi Tiết:**

**Power:**
```
1. UNO 5V          → Level Shifter HV (High Voltage)
2. ESP8266 3.3V    → Level Shifter LV (Low Voltage)
3. UNO GND         → Level Shifter GND (HV side)
4. ESP8266 GND     → Level Shifter GND (LV side)
```

**Data:**
```
5. UNO D1 (TX)     → Level Shifter HV1
6. Level Shifter LV1 → ESP8266 GPIO3 (RX)
7. UNO D0 (RX)     ← Level Shifter HV2
8. Level Shifter LV2 ← ESP8266 GPIO1 (TX)
```

**Ưu điểm:**
- ✅ Bi-directional (2 chiều)
- ✅ Tự động chuyển đổi
- ✅ Bảo vệ cả 2 phía
- ✅ Dùng được cho I2C, SPI

---

## 📌 PHẦN 2: POWER SUPPLY - NGUỒN ĐIỆN

### ⚡ Yêu Cầu Nguồn

**UNO:**
- Input: 7-12V DC (khuyên 9V)
- Cung cấp: 5V regulated (cho sensors, servos)
- Current: ~500mA

**ESP8266:**
- Input: 3.3V regulated
- Current: 80mA (idle), 170mA (Wi-Fi TX), 250mA peak
- ⚠️ **KHÔNG DÙNG CHUNG 5V UNO!**

### 🔌 Option 1: Nguồn Riêng (Khuyên Dùng)

```
Power Adapter 9V 2A
    │
    ├─── UNO Vin (7-12V)
    │     └─── UNO 5V out → Sensors, Servos
    │
    └─── AMS1117-3.3V Regulator
          └─── ESP8266 VIN/3V3
```

**AMS1117-3.3V Module:**
- Input: 4.5-7V
- Output: 3.3V @ 800mA
- Cost: ~5,000đ

**Nối:**
```
1. Power 9V (+)  → UNO Vin
2. Power 9V (+)  → AMS1117 VIN
3. AMS1117 VOUT  → ESP8266 3V3
4. Power GND     → UNO GND → ESP8266 GND (COMMON GND)
```

### 🔌 Option 2: Dùng USB (Test Only)

```
USB 5V (Computer)
    │
    ├─── UNO USB → 5V for UNO
    │
    └─── USB to 3.3V Module
          └─── ESP8266 3V3
```

**⚠️ CHÚ Ý:**
- Không đủ current cho servos → dùng nguồn riêng cho servo
- Chỉ dùng cho test firmware

---

## 📌 PHẦN 3: UNO PIN CONNECTIONS

### 🔌 Arduino UNO R3 Pinout (v1.4)

**UART (Hardware Serial):**
```
D0 (RX) ← ESP8266 GPIO1 (TX)  [qua Level Shifter]
D1 (TX) → ESP8266 GPIO3 (RX)  [qua Voltage Divider hoặc Level Shifter]
```

**⚠️ QUAN TRỌNG: Upload Code**
- Khi upload code cho UNO, **BẮT BUỘC** ngắt dây D0/D1
- Nếu không: Upload FAILED!
- Cách: Dùng jumper/switch để dễ ngắt/nối

**Sensors (I2C):**
```
A4 (SDA) → BH1750 SDA, LCD1602 SDA
A5 (SCL) → BH1750 SCL, LCD1602 SCL
```

**Sensors (Analog):**
```
A0 → Flame Sensor (analog output)
A1 → Sound Sensor (analog output)
A2 → Soil Moisture (analog output)
A3 → MQ-3 Gas Sensor (analog output)
```

**Sensors (Digital):**
```
D2 (ECHO) ← JSN-SR04T ECHO
D3 (TRIG) → JSN-SR04T TRIG
```

**Actuators (Relays - Active LOW):**
```
D7  → Relay 1 (Pump) [Active LOW]
D10 → Relay 2 (LED 12V Grow Light) [Active LOW]
```

**Actuators (Servos):**
```
D5 → Servo 1 (Door MG90) PWM
D6 → Servo 2 (Window MG996R) PWM
```

**DFPlayer Mini (SoftwareSerial):**
```
D11 (TX) → DFPlayer RX
D12 (RX) ← DFPlayer TX
```

**WS2812 LED Ring #1:**
```
D13 → WS2812 Ring #1 (8 LEDs) DIN
```

**Button:**
```
D4 ← Button (INPUT_PULLUP) → GND when pressed
```

---

## 📌 PHẦN 4: ESP8266 PIN CONNECTIONS

### 🔌 ESP8266 NodeMCU Pinout (v1.4)

**UART (Hardware Serial - UNO Communication):**
```
GPIO1 (TX0) → UNO D0 (RX)  [qua Level Shifter nếu dùng]
GPIO3 (RX0) ← UNO D1 (TX)  [qua Voltage Divider/Level Shifter BẮT BUỘC]
```

**DHT11 Sensor:**
```
GPIO4 (D2) → DHT11 DATA
DHT11 VCC  → 3.3V
DHT11 GND  → GND
```

**GPS NEO-6M (SoftwareSerial):**
```
GPIO13 (D7) ← NEO-6M TX
GPIO15 (D8) → NEO-6M RX
NEO-6M VCC  → 3.3V
NEO-6M GND  → GND
```

**Relays (Active LOW):**
```
GPIO5 (D1)  → Relay Fan [Active LOW]
GPIO16 (D0) → Relay AuxFan [Active LOW]
```

**WS2812 LED Ring #2 (NeoPixelBus UART Method):**
```
GPIO2 (D4) → WS2812 Ring #2 (8 LEDs) DIN
```

**⚠️ LƯU Ý GPIO:**
- GPIO0, GPIO2, GPIO15: Boot mode pins - KHÔNG dùng relay/switch
- GPIO16: Không có interrupt, không dùng cho ECHO sensor
- GPIO1/3 (TX0/RX0): Dùng cho UNO communication

---

## 📌 PHẦN 5: COMMON GROUND & POWER RAILS

### 🔌 Ground Bus (QUAN TRỌNG!)

**Tất cả GND phải nối chung:**
```
Power Supply GND
    │
    ├─── UNO GND
    │     ├─── Sensors GND
    │     ├─── Servos GND
    │     ├─── Relays GND
    │     └─── DFPlayer GND
    │
    ├─── ESP8266 GND
    │     ├─── DHT11 GND
    │     ├─── GPS GND
    │     └─── WS2812 Ring #2 GND
    │
    └─── Level Shifter GND (both sides)
```

**⚠️ QUAN TRỌNG:**
- Không có common GND → UART không hoạt động
- Noise trên GND → data corruption
- Giải pháp: Dùng dây ngắn, dày cho GND

### ⚡ Power Rails

**5V Rail (từ UNO):**
```
UNO 5V
    │
    ├─── BH1750 VCC
    ├─── LCD1602 VCC
    ├─── JSN-SR04T VCC
    ├─── Flame Sensor VCC
    ├─── Sound Sensor VCC
    ├─── Soil Moisture VCC
    ├─── MQ-3 VCC
    ├─── DFPlayer VCC
    ├─── WS2812 Ring #1 VCC
    ├─── Servos VCC (5V)
    └─── Level Shifter HV
```

**3.3V Rail (từ AMS1117 hoặc ESP8266):**
```
AMS1117 3.3V OUT
    │
    ├─── ESP8266 VIN/3V3
    ├─── DHT11 VCC
    ├─── GPS NEO-6M VCC
    ├─── WS2812 Ring #2 VCC
    └─── Level Shifter LV
```

---

## 📌 PHẦN 6: RELAY MODULE WIRING

### 🔌 4-Channel Relay Module

**Module Specs:**
- Active LOW trigger (5V = OFF, 0V = ON)
- Isolation: Optocoupler
- Load: 10A @ 250VAC, 10A @ 30VDC

**Control Pins:**
```
Relay Module                Arduino/ESP8266
────────────────────────    ───────────────
VCC (5V)                 ← UNO 5V
GND                      ← UNO GND
IN1 (Pump)               ← UNO D7
IN2 (LED 12V)            ← UNO D10
IN3 (Fan)                ← ESP8266 GPIO5 (D1)
IN4 (AuxFan)             ← ESP8266 GPIO16 (D0)
```

**Load Connections (COM/NO/NC):**
```
Relay 1 (Pump):
  COM ← Power Supply (+)
  NO  → Pump (+)
  NC  (not connected)

Relay 2 (LED 12V):
  COM ← Power Supply (+)
  NO  → LED Strip (+)
  NC  (not connected)

Relay 3 (Fan):
  COM ← Power Supply (+)
  NO  → Fan (+)
  NC  (not connected)

Relay 4 (AuxFan):
  COM ← Power Supply (+)
  NO  → AuxFan (+)
  NC  (not connected)

All loads: (-) → Power Supply GND
```

---

## 📌 PHẦN 7: TROUBLESHOOTING - XỬ LÝ SỰ CỐ

### 🔴 UART Không Hoạt Động

**Triệu chứng:** ESP8266 không nhận data từ UNO

**Kiểm tra:**
1. ✅ Common GND? (đo continuity)
2. ✅ Voltage divider đúng? (đo 3.3V ở ESP8266 RX)
3. ✅ TX/RX có bị ngược? (hoán đổi thử)
4. ✅ Baud rate đúng 115200? (check code)

**Test:**
```cpp
// UNO - test TX
void loop() {
  Serial.println("TEST");
  delay(1000);
}

// ESP8266 - test RX
void loop() {
  if (Serial.available()) {
    String msg = Serial.readStringUntil('\n');
    // Flash LED nếu nhận được
  }
}
```

---

### 🔴 Upload Code Failed (UNO)

**Triệu chứng:** `avrdude: stk500_recv(): programmer is not responding`

**Nguyên nhân:** D0/D1 bị chiếm bởi ESP8266

**Giải pháp:**
1. Ngắt dây D0 và D1 khỏi ESP8266
2. Upload code
3. Nối lại D0 và D1
4. Reset UNO

**Giải pháp vĩnh viễn:**
- Dùng jumper hoặc DIP switch trên breadboard
- Khi upload: OFF switch
- Khi chạy: ON switch

---

### 🔴 ESP8266 Bị Hư (GPIO3)

**Triệu chứng:** ESP8266 không boot, LED nhấp nháy lỗi

**Nguyên nhân:** Nối trực tiếp UNO TX 5V → ESP8266 GPIO3

**Giải pháp:**
- ❌ Không có - ESP8266 đã hư vĩnh viễn
- ✅ Mua ESP8266 mới
- ✅ NHỚ dùng voltage divider lần sau!

**Phòng tránh:**
- LUÔN LUÔN dùng voltage divider hoặc level shifter
- Đo voltage trước khi nối
- Kiểm tra 2 lần trước khi cấp nguồn

---

## 📌 PHẦN 8: CHECKLIST TRƯỚC KHI CẤP NGUỒN

### ✅ Pre-Power Checklist

**Hardware:**
- [ ] Level shifting 5V→3.3V đã setup (voltage divider hoặc module)
- [ ] Common GND đã nối đầy đủ
- [ ] Đo voltage ở ESP8266 RX: phải ≤ 3.6V
- [ ] ESP8266 nguồn 3.3V (KHÔNG PHẢI 5V!)
- [ ] UNO D0/D1 có jumper để dễ ngắt khi upload
- [ ] Polarity đúng (+/- không bị ngược)

**Software:**
- [ ] Wi-Fi SSID/Password đã cập nhật trong code
- [ ] MQTT broker address đã đúng
- [ ] Baud rate: 115200 cho cả UNO và ESP8266

**Safety:**
- [ ] Không có short circuit (đo continuity giữa 5V và GND)
- [ ] Relay load không quá 10A
- [ ] Servo không quá 2A total
- [ ] USB cable chất lượng tốt (data + power)

---

## 📌 PHẦN 9: FIRST BOOT - KHỞI ĐỘNG LẦN ĐẦU

### 🚀 Sequence

**Bước 1: Upload Firmware**
```
1. Ngắt D0/D1 của UNO (jumper OFF)
2. Upload UNO firmware qua Arduino IDE
3. Nối lại D0/D1 (jumper ON)
4. Upload ESP8266 firmware qua Arduino IDE
5. Mở Serial Monitor → kiểm tra boot messages
```

**Bước 2: Test UART Communication**
```
1. Subscribe MQTT: greenhouse/sys/debug
2. Cấp nguồn cho UNO → đợi boot
3. Cấp nguồn cho ESP8266 → đợi Wi-Fi connect
4. Kiểm tra MQTT debug messages:
   - "UNO: UNO v1.4 started"
   - "UNO: BH1750 init OK"
   - "ESP8266: MQTT connected"
```

**Bước 3: Test Sensors**
```
Subscribe: greenhouse/data/#
Expect:
  - temperature: {"v":25.5,"u":"°C","t":12345}
  - humidity: {"v":68.0,"u":"%","t":12346}
  - soilMoisture: {"v":45.2,"u":"%","t":12347}
  ...
```

**Bước 4: Test Actuators**
```
Publish: greenhouse/control/fan → "ON"
Expect:
  - Relay click sound
  - Status: greenhouse/status/fan → "ON"
  - Debug: "ESP8266: Fan: ON"
```

---

## 📌 PHẦN 10: TROUBLESHOOTING ADVANCED

### 🔍 UART Data Corruption

**Triệu chứng:** Nhận được data nhưng bị lỗi JSON parse

**Nguyên nhân:**
1. Ground loop noise
2. Dây UART quá dài
3. Không có pull-up resistor

**Giải pháp:**
```
1. Rút ngắn dây UART (<10cm tốt nhất)
2. Dùng twisted pair hoặc shielded cable
3. Thêm 10kΩ pull-up ở ESP8266 RX lên 3.3V
4. Giảm baud rate xuống 57600 thử
5. Thêm 100nF capacitor giữa VCC và GND (noise filter)
```

---

**HẾT PHẦN WIRING GUIDE**

**Next:** Xem `V1.4_USAGE_GUIDE.md` để biết cách sử dụng hệ thống!
